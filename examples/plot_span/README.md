# Plot Span Demo

This example demonstrates how to add spans to a plot. Spans are shaded regions that can highlight ranges on either the X or Y axis, with customizable borders, colors, and labels.

## Running

Native
```sh
cargo run -p plot_span
```

Web (WASM)
```sh
cd examples/plot_span
trunk serve
```

![](screenshot.png)
